const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav-list');if(toggle&&nav){toggle.addEventListener('click',()=>{nav.classList.toggle('open');toggle.setAttribute('aria-expanded',nav.classList.contains('open'))})}
const form=document.querySelector('#contactForm');if(form){const message=document.querySelector('.form-message');form.addEventListener('submit',e=>{e.preventDefault();const name=form.name.value.trim();const phone=form.phone.value.trim();const email=form.email.value.trim();if(!/^[A-Za-zÀ-ÿ\s]+$/.test(name)){message.textContent='Digite apenas letras no nome.';return}if(!/^\d{10,11}$/.test(phone)){message.textContent='Digite um telefone com 10 ou 11 números.';return}if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){message.textContent='Digite um e-mail válido, como exemplo@gmail.com.';return}message.textContent='Formulário validado. Para envio real, conecte um serviço como Formspree.';form.reset()})}
document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
